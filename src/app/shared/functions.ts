// Reutrns whether the application window is small enough to be switched to a view more suited for mobile screens
export function isMobile(): boolean {
  // return window.innerWidth <= 950;
  return false;
}
